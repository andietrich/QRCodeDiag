<UserControl
    x:Class="QRCodeDiagUWP.CodeOptions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QRCodeDiagUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel>
        <TextBox Header="{x:Bind Path=CodeSymbolName}" Text="{x:Bind Path=symbolValue}" IsReadOnly="True"/>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <CheckBox Grid.Row="1" Grid.Column="0" Content="Draw code element" IsChecked="{x:Bind Path=DrawSymbolCode, Mode=TwoWay}"/>
            <CheckBox Grid.Row="1" Grid.Column="1" Content="Draw bit indices" IsChecked="{x:Bind Path=DrawBitIndices, Mode=TwoWay}"/>
            <CheckBox Grid.Row="2" Grid.Column="0" Content="Draw symbol indices" IsChecked="{x:Bind Path=DrawSymbolIndices, Mode=TwoWay}"/>
            <CheckBox Grid.Row="2" Grid.Column="1" Content="Draw symbol values" IsChecked="{x:Bind Path=DrawSymbolValues, Mode=TwoWay}"/>
        </Grid>
    </StackPanel>
</UserControl>
